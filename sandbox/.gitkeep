# This file keeps the sandbox/ directory tracked in git.
# All generated project subfolders inside sandbox/ are excluded via .gitignore.
# Users' projects are stored locally on their own machines only.
